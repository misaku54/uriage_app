<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container-fluid">
    <%= link_to "LiLy", root_path, id: "logo" %>
    <nav>
      <div class="navbar-header">
        <button id="hamburger" type="button" class="navbar-toggle collapsed">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
      </div>
      <ul id="navbar-menu"
          class="nav navbar-nav navbar-right collapse navbar-collapse">
        <% if logged_in? %>
          <% if current_user.admin? %>
            <li class="dropdown">
				    	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">管理者 <span class="caret"></span></a>
				    	<ul class="dropdown-menu" role="menu">
				    		<li><%= link_to "ユーザー管理", admin_users_path %></li>
				    	</ul>
				    </li>
          <% else %>
            <li><%= link_to "売上一覧", user_sales_path(current_user) %></li>
            <li class="dropdown">
				    	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button">運用管理 <span class="caret"></span></a>
				    	<ul class="dropdown-menu" role="menu">
				    		<li><%= link_to '商品マスタ', user_producttypes_path(current_user) %></li>
				    		<li><%= link_to 'メーカーマスタ', user_makers_path(current_user) %></li>
				    	</ul>
				    </li>
          <% end %>
          <li class="dropdown">
	          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"><%= current_user.name %>さん<span class="caret"></span></a>
				  	<ul class="dropdown-menu" role="menu">
				  		<li><%= link_to "ユーザー情報", current_user %></li>				  		
              <li class="divider"></li>
				  		<li><%= link_to "ログアウト", logout_path, data: { "turbo-method": :delete, turbo_confirm: 'ログアウトしますか？' } %></li>
				  	</ul>
				  </li>
        <% else %>
          <li><%= link_to "ログイン", login_path %></li>
        <% end %>
      </ul>      
    </nav>
  </div>
</header>